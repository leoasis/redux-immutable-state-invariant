{
  "name": "object-invariant-test-helper",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "babel --out-dir dist src",
    "test": "mocha --compilers js:babel/register",
    "prepublish": "npm run clean && npm run build"
  },
  "author": "Jana Beck <jana@tidepool.org>",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.3",
    "expect": "^1.8.0",
    "mocha": "^2.2.5"
  }
}
